#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: a pointer to a string
 *
 * Return: the length of a string as int
 */

int _strlen(char *s)
{
	return(strlen(s));
}
